services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data/uploads:/app/backend/uploads
      - ./data/db:/app/backend/db
    env_file: .env
    environment:
      - PORT=3000
    restart: unless-stopped
